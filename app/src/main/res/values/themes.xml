<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AsteroidRadar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/Primary</item>
        <item name="colorOnPrimary">@color/OnPrimary</item>
        <item name="colorPrimaryContainer">@color/PrimaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/OnPrimaryContainer</item>

        <item name="android:windowBackground">@color/Background</item>

        <item name="android:textColor">@color/OnPrimary</item>

    </style>

    <style name="Theme.AsteroidRadar" parent="Base.Theme.AsteroidRadar" />

    <style name="text_base" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:background">@color/Background</item>
        <item name="android:textColor">@color/Text</item>
        <item name="android:textSize">@dimen/text_size_medium</item>
    </style>

    <style name="text_over_image" parent="text_base">
        <item name="android:paddingStart">@dimen/padding_normal</item>
        <item name="android:paddingEnd">@dimen/padding_normal</item>
        <item name="android:paddingTop">@dimen/padding_small</item>
        <item name="android:paddingBottom">@dimen/padding_small</item>

        <item name="android:textColor">@color/TextSecondary</item>
    </style>

    <style name="list_entry" parent="text_base">
        <item name="android:paddingBottom">@dimen/padding_normal</item>
    </style>
    
    <style name="asteroid_element" parent="text_base">
    </style>

    <style name="asteroid_headline" parent="asteroid_element">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="detail_text" parent="text_base">
    </style>
</resources>